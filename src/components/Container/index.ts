import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;
  height: 100vh;
  position: relative;  
  padding: 0px 0px 0px var(--primary-padding);
  overflow: hidden;
  z-index: 0;

  align-self: flex-start;

  display: flex;
  flex-flow: row wrap;
  justify-content: flex-end;
  align-items: flex-end;
`
